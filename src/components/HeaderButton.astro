---
  interface HeaderButtonProps {
    enalce: string;
  }

  const { enlace } = Astro.props
---

<a
href={enlace}
  class="py-2 px-4 hover:bg-red-800 focus:ring-red-800 cursor-pointer
        focus:ring-offset-indigo-200 text-white w-full transition ease-in
        duration-200 text-center focus:outline-none focus:ring-2 focus:ring-offset-2 rounded-lg"
>
  <slot />
</a>
